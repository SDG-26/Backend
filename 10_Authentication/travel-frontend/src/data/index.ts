import { createPost, getPosts, getSinglePost } from './posts';
import { register, login, logout, getMe } from './auth';

export { createPost, getPosts, getSinglePost, register, login, logout, getMe };
